
---

import agentFlowSvgUrl from "../assets/AgentFlow.svg?url";

const bullets = [
    "YAML一枚でワークフローを定義",
    "スクリプト・LLMを順次/並列化でシームレス連携"
];
const bulletsEn = [
    "Define workflows in a single YAML file",
    "Seamlessly orchestrate scripts and LLMs in sequential or parallel execution"
];
---
<section id="agentflow" class="af-section">
    <div class="af-container">
        <!-- 左：SVG（PC）／上：SVG（SP） -->
        <div class="visual-wrap">
            <div class="af-glow" aria-hidden="true"></div>
            <figure class="af-frame" aria-label="AgentFlow emblem">
                <img
                    src={agentFlowSvgUrl}
                    alt="AgentFlow Orchestration"
                    class="af-svg"
                    loading="lazy"
                    decoding="async"
                />
            </figure>
        </div>

        <!-- 右：テキスト（PC）／下：テキスト（SP） -->
        <header class="af-head">
            <p class="eyebrow">Automation Framework</p>
            <h2 class="title">
                AgentFlow v0.3
            </h2>
            <p class="lede">
                「喋らせるな、働かせろ。」<br />
                LLM・スクリプトを<span>“働かせる”</span>ための軽量基盤。<br /><br />
                <span class="en">
                    “Don’t make them talk — make them work.”<br />
                    A lightweight Python framework to make LLMs and scripts actually <em>work</em>.
                </span>
            </p>
            <ul class="bullets">
                {bullets.map((b, i) => (
                    <li>
                        {b}<br />
                        <span class="en">{bulletsEn[i]}</span>
                    </li>
                ))}
            </ul>
            <div class="cta-row">
                <a
                    href="https://github.com/YusukeUtsumi/agentflow/tree/release/v0.3"
                    target="_blank"
                    rel="noreferrer"
                    class="cta"
                >
                    View on GitHub
                </a>
            </div>
        </header>
    </div>
</section>
