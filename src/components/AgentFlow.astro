---
import "../styles/agentflow.css";

const bullets = [
    "YAML一枚でワークフロー/エージェントを定義",
    "LLM・RPA・外部APIをシームレス連携",
    "明示的なツール呼び出しと制約管理",
];
---
<section id="agentflow" class="af-section">
    <div class="af-container">
        <!-- 左：SVG（PC）／上：SVG（SP） -->
        <div class="visual-wrap">
            <div class="af-glow" aria-hidden="true"></div>
            <figure class="af-frame" aria-label="AgentFlow emblem">
                <img
                    src="/images/AgentFlow.svg"
                    alt="AgentFlow Orchestration"
                    class="af-svg"
                    loading="lazy"
                    decoding="async"
                />
            </figure>
        </div>

        <!-- 右：テキスト（PC）／下：テキスト（SP） -->
        <header class="af-head">
            <p class="eyebrow">Automation Framework</p>
            <h2 class="title">AgentFlow</h2>
            <p class="lede">
                フロー定義はYAMLに集約。<br />
                LLM・スクリプト・APIを<span>“働かせる”</span>ための軽量基盤。
            </p>
            <ul class="bullets">
                {bullets.map((b) => <li>{b}</li>)}
            </ul>
            <div class="cta-row">
                <a
                    href="https://github.com/YusukeUtsumi/agentflow/tree/release/v0.3"
                    target="_blank"
                    rel="noreferrer"
                    class="cta"
                >
                    GitHubを見る
                </a>
            </div>
        </header>
    </div>
</section>
