---
import "../styles/hero.css";
---
<section id="hero" class="hero" aria-labelledby="hero-title">
    <div class="hero-wrap" data-orbs-desktop="16" data-orbs-mobile="4">
        <!-- 左：実写（装飾扱いなら alt=""） -->
        <div class="hero-left">
            <picture class="hero-media">
                <source srcset="/images/hero_risa.webp 1200w, /images/hero_risa.webp 1200w" type="image/avif" />
                <img
                    src="/images/hero_risa.webp"
                    alt=""
                    width="1200"
                    height="1600"
                    decoding="async"
                    loading="eager"
                    fetchpriority="high"
                    sizes="(max-width: 960px) 100vw, 50vw"
                />
            </picture>
        </div>

        <!-- 右：ファンタジー -->
        <div class="hero-right">
            <picture class="hero-media">
                <source srcset="/images/hero_lilia.webp 1200w, /images/hero_lilia.webp 1200w" type="image/avif" />
                <img
                    src="/images/hero_lilia.webp"
                    alt=""
                    width="1200"
                    height="1600"
                    decoding="async"
                    loading="eager"
                    sizes="(max-width: 960px) 100vw, 50vw"
                />
            </picture>
        </div>

        <!-- オーブ -->
        <div class="orbs" aria-hidden="true">
            <span class="orb" style="--x:18; --y:28; --sz:180px; --h:28;  --a:.85; --d1:13s; --d2:20s; --k:1;"></span>
            <span class="orb" style="--x:74; --y:42; --sz:220px; --h:36;  --a:.75; --d1:15s; --d2:23s; --k:-1;"></span>
            <span class="orb" style="--x:48; --y:68; --sz:140px; --h:12;  --a:.70; --d1:12s; --d2:18s; --k:1;"></span>
            <span class="orb" style="--x:30; --y:78; --sz:200px; --h:20;  --a:.54; --d1:18s; --d2:26s; --k:1;"></span>
        </div>

        <!-- 中央テキスト -->
        <div class="hero-content">
            <h1 id="hero-title" class="hero-title">
                <span>AIはカーネーションの<br/>夢を見るか？</span><br/>
                <p class="hero-sub">Do AIs Dream of Carnations?</p>
            </h1>
            <!--<p class="hero-sub">Do AIs Dream of Carnations?</p>-->
        </div>
    </div>

    <script type="module" src="/src/scripts/hero.ts"></script>
</section>
